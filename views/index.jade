extends layout

block content
  h1= title
  p Welcome to #{title}
  table(cellpadding="10", cellspacing="0", border="1", bordorcolor="black", borderstyle="solid")
    each item in products
      tr
        td #{item.title}
        td #{item.description}
        td #{item.price}
